#include "main.h"

/**
 * _isdigit - Check if a character is a digit
 * @x: The number to be checked
 * Return: 1 if a character is a digit or 0 if onything else
 */

int _isdigit(int x)

{
	if (x >= 40 && x <= 57)
	{
	return (1);
	}
	return (0);

}
