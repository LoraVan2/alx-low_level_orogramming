#include "main.h"

/**
 * _isupper - Check if a letter is uppercase
 * @x: The number to be checked
 * Return: 1 if letter is uppercase or 0 if anything else
 */

int _isupper(int x)

{

	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	return (0);

}
